# Frames & units contract

Freeze this **before** CAD, firmware zeroing, or sim. Everything downstream trusts these names.

## Conventions (fill in as the design locks)

| Item | Decision |
|------|----------|
| World / gravity frame | TBD (e.g. Z-up, right-hand) |
| Base / pelvis frame | TBD |
| Joint axis sense | Right-hand rule; positive = TBD |
| Encoder zero | Mechanical stop / mid-range / calibrated home — TBD per joint |
| IMU mount link / extrinsic | TBD (Day 03); units $m/s^2$, $rad/s$ |
| Contact frames (feet/hands) | TBD (Day 03); wrench N, N·m |
| Lengths | meters |
| Angles | radians in software; degrees only in human docs |
| Mass / inertia | kg, kg·m² about link CoM |
| Torque / force | N·m, N |
| Time | seconds; loop rates in Hz; declare bus timebase |
| Voltage / current | V, A on named rails; publish bus and logic domains separately |

## Named links & joints

| Link | Parent | Joint | Axis | Limits (rad) | Notes |
|------|--------|-------|------|--------------|-------|
| — | — | — | — | — | Add first limb here |

## Message / bus rates (targets)

| Loop | Rate | Owner |
|------|------|-------|
| Current / FOC | 10–20 kHz | MCU |
| Joint torque / impedance | 1–2 kHz | MCU |
| Contact / FT sample | 0.5–2 kHz | MCU |
| IMU raw | 200–1000 Hz | MCU |
| State estimation | 100–500 Hz | mid compute |
| Behavior / planning | ≤50 Hz | mid / high compute |
| Power / BMS publish | 10–100 Hz (faster under fault) | BMS / mid MCU |

## Clock domain & bus (Day 04)

| Item | Decision |
|------|----------|
| Spine | TBD (CAN-FD / EtherCAT / hybrid) |
| Time master / `clock_domain` name | TBD (e.g. `ec_dc`, `host_mono`) |
| Cycle time $T_c$ | TBD |
| Node ID map | TBD (link from bus config) |
| `age_budget_ms` by message class | TBD — JointState / ImuSample / ContactState / PowerState / EstimatorState |
| Sync mechanism | TBD (DC / heartbeat / host-stamp early only) |

## Power domain & voltage (Day 05)

| Item | Decision |
|------|----------|
| Pack nominal / series count | TBD |
| High-power DC bus rail | TBD (e.g. pack-direct ~48 V class) |
| Logic / sensor rails | TBD (e.g. isolated 5 V / 3.3 V) |
| Soft-sag / soft-UV / UVLO thresholds | TBD — label measured vs illustrative |
| `torque_scale` / `max_iq_abs` consumer | Day 02 joint command path |
| STO / safe-torque-cut owner | TBD (hardware gate-power kill) |

## Estimation & contact modes (Day 06)

| Item | Decision |
|------|----------|
| Estimator owner / output rate | TBD (target 100–500 Hz; one writer of `EstimatorState`) |
| Process model class | TBD (EKF / complementary / hybrid) |
| Stale policy | TBD — hold last + `stale` vs process-model propagate |
| Contact mode list | TBD (e.g. FLIGHT / SS / DS) and how `contact_set` is derived |
| Power-aware residual rule | TBD — downweight / gate on `torque_scale` or `PowerFault` |
| Twin delay / dropout / sag hooks | TBD — must match hardware `age_budget_ms` and Day 05 derate |

## Upper-body / EE frames (Day 10)

| Item | Decision |
|------|----------|
| EE / wrist frames (L/R) | TBD — named links aligned with `EstimatorState` |
| Hand / grasp frames | TBD — aperture units; wrench N, N·m if instrumented |
| `ManipulationCommand` owner / rate | TBD (mid-rate; one writer of manip intent) |
| Hand role tags | FREE / CLAIM_SUPPORT / MEASURED_SUPPORT — measured `contact_set` owns truth |
| Mid-reach abort signals | stale / `POWER_GATED` / low `torque_scale` → abort/soften EE tasks |
| Twin reach / sag hooks | TBD — matched arm-load peaks and contact delay as hardware |

## Soft learned / teleop / arbiter contracts (Days 14–16)

Soft writers only. Measured `ContactState` still owns hard support membership into one WBC/QP. `never_hard` binds residual, scene, policy, teleop, and arbiter alike. Mid / host rate with `age_ms` — never µs / EtherCAT cyclic until jitter is measured.

| Item | Decision |
|------|----------|
| `LearnedResidual` / `ResidualCorrection` owner / rate | TBD (Day 14) — soft additive on estimate features / soft task refs / AffordanceProposal ranking only |
| `SoftSceneGraph` / Object / Support / Relation nodes | TBD (Day 14) — enrich AffordanceProposal CLAIM candidates only |
| `LearnedPolicy` / `PolicyAction` / `PolicyCommand` owner / rate | TBD (Day 15) — mid / host; soft refs / AffordanceProposal ranking / CLAIM admit only; `closed_loop: true`; `never_hard: true` |
| `SimToRealGate` status / checks | TBD (Day 15–16) — RED/YELLOW/GREEN; block free closed-loop / shared autonomy until GREEN |
| `TeleopCommand` / `OperatorIntent` source / rate | TBD (Day 16) — STICK / VR / EE_DELTA / BUTTON; mid / host; soft refs / CLAIM admit only; `never_hard: true` |
| `SharedAutonomyArbiter` modes | TELEOP_ONLY \| POLICY_ONLY \| BLEND / SHARED \| OPERATOR_OVERRIDE \| ABORT — **all still soft**; OVERRIDE ≠ hard ownership / MEASURE skip |
| Soft vs hard ownership | Teleop / policy / arbiter / residual / scene / affordance = soft; measured FOOT\|HAND\|OBJECT = hard cones + $F_z \ge 0$; one WBC/QP; yield to Day 08 gait / Day 09 recovery / Day 05 power |
| Mid-arbitration abort signals | stale / `POWER_GATED` / flicker / residual disagree / operator dropout / reward-hack / SimToReal fail → abort/demote |
| Twin mid-arbitration hooks | teleop vs policy vs MEASURE vs gait vs recovery fights; operator latency/dropout; false CLAIM from intent; reward-hack to hard write — must match hardware |
| Rate / jitter rule | No neural net **or** human stick path inside µs / EtherCAT cyclic until jitter measured |
